'use client'

/**
 * FloatingWidget — stub for Phase 1.
 *
 * Renders a fixed bottom-right overlay that will display the running timer
 * (Requirement 2.4). Full implementation comes in Phase 2 Task 6 alongside
 * the complete TimerProvider.
 *
 * The component is a Client Component because it will eventually read from
 * TimerContext (a client-side context) to decide whether to render.
 */
export function FloatingWidget() {
  // Phase 1 stub: render nothing until the timer is implemented.
  return null
}
